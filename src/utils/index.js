
const filterByBand = require('./filterByBand');
const groupByChannel = require('./groupByChannel');
const isBuffer = require('./isBuffer');
const voltsToMicrovolts = require('./voltsToMicrovolts');

module.exports = {
    filterByBand,
    groupByChannel,
    isBuffer,
    voltsToMicrovolts
};
